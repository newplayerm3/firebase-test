# Netlify 重定向规则
# 用于单页应用（SPA）路由支持

# 所有路由都重定向到 index.html，保持 URL 不变
/*    /index.html   200

# API 代理（如果需要）
# /api/*  https://your-api-domain.com/api/:splat  200

# 安全重定向
# 强制 HTTPS
http://your-domain.com/*  https://your-domain.com/:splat  301!

# 404 页面（可选）
# /404  /404.html  404
